apiVersion: v1 
kind: Service
metadata:
   name: my-svc
   labels:
     app: myapp
   namespace: hpa
spec: 
  type: ClusterIp
  selector:
    app: myapp
    ports:
      - name: my-svc-port
        protocol: TCP
        port: 80
        targetport: 80
