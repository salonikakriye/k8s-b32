apiVersion: v1
kind: Pod
metadata:
  name: mypod
  labels:
    app=myapp
    env=devops
spec:
 containers:
   - name: nginx-cont
     image: nginx
     ports:
      - name: mginxport
        containerport: 80
