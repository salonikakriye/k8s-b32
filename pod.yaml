apiVersion: v1
kind: Pod
metadata:
  name: mypod
  labels:
     app: myapp
spec:
  containers:
    - name: nginx-cont
      image: nginx
      ports:
       - name: nginxport
         containerPort: 80
